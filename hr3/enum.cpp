


#include <iostream>

using namespace std;

main() {
  enum Color { RED=100, BLUE, GREEN=500, WHITE, BLACK=700 };
  //cout << RED << endl;
  cout << "I set the following: enum Color { RED=100, BLUE, GREEN=500, WHITE, BLACK=700 }" << endl;
  cout << "Here is the variable RED= " << RED << endl;
  
  
  return 0;
}